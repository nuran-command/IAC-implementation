# ============================================================
# User Profile Service — Kubernetes Deployment + Service
# ============================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-profile-service
  namespace: sre-app
  labels:
    app: user-profile-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-profile-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: user-profile-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "80"
    spec:
      containers:
      - name: user-profile-service
        image: tiangolo/uvicorn-gunicorn-fastapi:python3.9
        ports:
        - containerPort: 80
        envFrom:
        - configMapRef:
            name: app-config
        - secretRef:
            name: app-secret
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: user-profile-service
  namespace: sre-app
spec:
  selector:
    app: user-profile-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
